const inquirer = require("inquirer");
const fs = require("fs");
const generateHTML = require("./utils/generateHTML.js");


function promptUser() {
  inquirer.prompt(questionsMng).then(function (answers) {
        if (answers.name === betsy) {
          inquirer.prompt
        } else {
          inquirer.prompt
        } => writeToFile("generateHTML", answers));
}
